import type { Workflow } from '../database/workflows'

export async function generateWorkflowEditView(workflow: Workflow) {
  return []
}

export async function generateWorkflowView(workflow: Workflow) {
  return []
}
